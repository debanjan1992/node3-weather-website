<!DOCTYPE html>

<head>
    {{>scripts}}
    <link rel="stylesheet" href="css/styles.css">
</head>

<body class="container">
    {{>header}}
    <div class="content">
        <p>Use this site to get the weather.</p>

        <form>
            <div class="form-group">
                <input type="text" class="inp-location form-control" id="location" placeholder="Enter location">
                <small class="form-text text-muted">We'll never share your data with anyone
                    else.</small>
                <div class="alert alert-danger mt-2" id="error"></div>
            </div>
            <button type="submit" class="btn btn-primary">Submit</button>
        </form>
        <div class="output">
            <div class="alert alert-primary" id="message1"></div>
            <div class="alert alert-primary" id="message2"></div>
        </div>
    </div>
    {{>footer}}
    <script type="text/javascript" src="js/app.js"></script>
</body>

</html>